mutation login($loginData: userInput!) {
  login(loginData: $loginData) {
    accessToken
    error {
      userId
      message
    }
    user {
      _id
      username
      role
      loggedIn
    }
  }
}
